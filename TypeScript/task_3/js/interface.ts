// Define RowID type alias
export type RowID = number;

// Define RowElement interface structure layout
export interface RowElement {
  firstName: string;
  lastName: string;
  age?: number; // Optional attribute
}
